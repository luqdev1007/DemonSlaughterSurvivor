# CLAUDE.md

Точка входа для Claude. Читается в начале каждой сессии. Держать коротким — всё, что растёт, живёт в других файлах.

---

## 1. Что это за проект

3D-роглайт-выживание: орды врагов, автоатака, апгрейды внутри забега, мета-прогрессия между забегами. Отличия от Vampire Survivors — ближний бой в 3D, **активные способности** под ручным управлением и **ростер персонажей** с разным геймплеем.

**Design pillar: «Автоатака ведёт бой — игрок решает моменты».**
Всё, что игрок делает руками — позиционирование, рывок, ульта, артефакт — про выбор момента, а не про исполнение комбо. Pillar отсекает и файтинговые комбо-системы, и полную автоматизацию в духе VS.

Рабочее название: **Demon Slaughter Survivor** (репозиторий назван с опечаткой — `Deman-Slaughter-Survivor`).
Платформа: PC (Steam), Windows в приоритете. Build target — StandaloneWindows64, API level .NET Standard 2.1.
Unity: **6000.4.11f1**
Корень namespace и имён сборок: **`Game.*`**.
Solo-разработка, учебный формат (см. §2). Планируются пострелизные обновления — гибкость систем не опциональна.

---

## 2. Формат работы — ГЛАВНОЕ ПРАВИЛО

**Код пишет агент целиком. Разработчик обсуждает, принимает решения, тестирует руками.**

Формат сменён 2026-09-24. История:
- до 2026-09-06 — код пишет разработчик, Claude выдаёт конспект. Вырождался в списывание готового решения;
- 2026-09-06 → 2026-09-24 — агент пишет базу, разработчик заполняет дырки;
- с 2026-09-24 — текущий. Причина: целевой навык — работать с готовым кодом и направлять ИИ, а не писать с чистого листа. Заполнение дырок этот навык не тренирует.

Обратно не откатывается без явного решения.

### Роль агента

- Пишет весь код блока. Учебных дырок нет. `throw new NotImplementedException()` допустим только как явная заглушка функциональности будущего шага, с пометкой в отчёте.
- **До кода — план блока:** компоненты, системы, группы из `ARCHITECTURE.md` §10, затрагиваемые файлы. Код начинается после подтверждения плана.
- **Развилки не решаются молча.** Если у решения минимум два разумных варианта с разной ценой, агент называет варианты, цену и свою рекомендацию. Выбирает разработчик. Однозначные решения агент принимает сам и фиксирует в отчёте.
- **Спорные решения.** Видишь подход хуже альтернативы — говори прямо и до начала работы. Соглашаться из вежливости нельзя.
- Объяснения по запросу — на коде проекта. Запрет примеров на сущностях проекта снят вместе с дырками.

### Роль разработчика

- Утверждает план и решает развилки.
- **Приёмка блока — руками в play mode** по сценарию из отчёта (§6 п. 7). Блок без ручной проверки не закрыт.
- **В `PROGRESS.md` блок помечается закрытым только после ручной приёмки.** До неё формулировка — «код готов, ждёт ручной приёмки», с датой готовности кода. Иначе «закрыто» в журнале означает «агент так считает», и отличить проверенное от непроверенного через месяц будет нечем.
- Ревьюит на уровне «почему так». Непонятно, где живёт решение — повод спросить, а не принять.

### Понимание кода — обязательная часть отчёта

Разработчик не пишет код, но обязан ориентироваться в нём. Поэтому каждый отчёт содержит карту блока: путь данных от конфига/ввода через системы до результата, с именами файлов. Цель — чтобы через месяц можно было найти любое место за пять минут, не читая всё подряд.

**Язык общения — русский.**

---

## 3. Карта документов

Читать по необходимости, не держать всё в контексте.

| Файл | Что внутри | Когда читать |
|---|---|---|
| `CLAUDE.md` | Этот файл: правила, стек, конвенции | Всегда |
| `docs/PROGRESS.md` | Текущий этап, роадмап, журнал решений | Всегда |
| `docs/ARCHITECTURE.md` | Слои, границы ECS, DI, пайплайн статов | Перед задачей по коду |
| `docs/GDD.md` | Механики, режимы, персонажи, экономика | Перед задачей по геймплею |
| `docs/LORE.md` | Сюжет, персонажи, нейминг, юр. ограничения | Перед задачей по контенту |
| `docs/recons/` | Отчёты по завершённым сессиям | При подхвате незакрытой работы |

**Доки — намерение (intent). Код — источник истины.** Документы неизбежно отстают от кода. Расходится код с докой — **верь коду и явно флагай расхождение**, не подгоняй код под доку молча и не строй на устаревшем допущении.

Отсюда обязанность держать `ARCHITECTURE.md` коротким: документ, который дорого обновлять, перестают обновлять — и он начинает врать.

---

## 4. Стек

Зафиксировать точные версии и коммиты.

| Пакет | Версия / коммит | Роль |
|---|---|---|
| VContainer | git `5103bfc467dbb3d7b995dd2f2c5944e9f077dc29` | DI вне ECS: сервисы, сцены, UI, сейвы |
| LeoECS Lite | git `d46f3b209ea163810823aeea7d2610d38ad6cdf8` | Ядро симуляции |
| LeoECS Lite DI | git `482b14e87c09cd1bf3f8f3dffc5c0099f00a693a` | Инъекция shared-данных в системы |
| NuGetForUnity | git `acc1c7bc9ea34c33b830e40316fca52553878d29` | Доставка .NET-зависимостей |
| UniTask | git `e5acc106ee196bc5a32fb14cdf2987b0f96d11e0` | Async: загрузка, тайминги |
| R3 | git `3fed50ae5c7e123073f6e50218b2a0e6310d50b4`; ядро R3 из NuGet — 1.3.1 | Реактивность на границе UI/меты |
| DOTween | 1.2.825, `Assets/Plugins/Demigiant` (precompiled dll) | Твины UI и фидбэка |
| Cinemachine | 3.1.7 (UPM) | Камера |
| Unity Input System | 1.19.0 (UPM) | Активные способности, геймпад, ребайндинг |
| MCP for Unity (CoplayDev) | git `4ce7dd3cc54e37e2ed6dc59cb5a047f3dccb3f50` | Доступ Claude к проекту |

Стоит в проекте, но в архитектуре роли пока не имеет: URP 17.4.0, Addressables 2.9.1, Test Framework 1.6.0, AI Navigation 2.0.13, Timeline 1.8.12, Visual Scripting 1.9.11, Multiplayer Center 1.0.1.

**Важно:** LeoECS Lite менял хостинг репозитория. В `manifest.json` — конкретный коммит, не ветка. То же для MCP.

Все семь git-пакетов запинены по SHA 2026-08-19. Обновление пакета = осознанная смена хеша в `manifest.json` отдельным коммитом, а не побочный эффект `Resolve`.

---

## 5. Конвенции кода

- **Язык кода — английский.** Идентификаторы, комментарии, коммиты, имена файлов.
- **Namespace:** `Game.<Layer>.<Feature>`.
- **Компоненты ECS:** существительное без суффикса — `Health`, `MoveSpeed`, `Facing`.
- **Теги:** `Player`, `Enemy`, `Dead`.
- **События (один кадр):** суффикс `Event`, чистятся системой очистки.
- **Системы:** `ApplyDamageSystem`, `SpawnWaveSystem`.
- **Конфиги:** ScriptableObject, суффикс `Config`.
- **Компоненты одной темы — один файл.** `MovementComponents.cs`, `CommonComponents.cs`, `TagComponents.cs`.
- **Приватные поля:** `_camelCase`.
- **Классы, не рассчитанные на наследование, — `sealed`.**
- **Никаких однострочных `if`** — условие и действие на разных строках всегда.
- **Комментарии `//` и XML-документация `///` в коде не пишутся** (решение 2026-08-29, отчёт 010 §3 Р7). Исключения: `RunSystems.cs`, где комментарии выражают 13 групп порядка систем из `ARCHITECTURE.md` §10, и сгенерированные файлы. Обоснования решений живут в отчётах `docs/recons/`, журнале `PROGRESS.md` и справочнике.
- `[FormerlySerializedAs]` при переименовании сериализованных полей.
- **Assembly Definitions обязательны.**

---

## 6. Правила работы с проектом

1. **Не угадывай архитектуру.** Неясно из доков — читай код через MCP. Всё равно неясно — **спрашивай, не изобретай**.
2. **Не описывай поведение фреймворка по памяти.** Любое утверждение о том, как ведёт себя Unity, VContainer или LeoECS Lite, подтверждается либо чтением исходников пакета, либо экспериментом в песочнице. Песочница удаляется после проверки. Нарушение этого правила — самая частая причина ошибок в отчётах 002, 003, 004, 007.
3. **Проверяй существующую инфраструктуру перед новой.** До предложения нового сервиса/фабрики/менеджера — убедись, что параллельного уже нет. Дублирующие сущности — главный источник гнили в долгоживущем проекте.
4. **Тайтовый скоуп.** Не расширяй поверхность задачи. Файлы вне скоупа не трогай без подтверждения. Заметил проблему рядом — скажи, но не чини по ходу.
5. **Одно изменение — одна причина.** Рефакторинг и новая фича не едут в одном заходе.
6. **Проверяй границу MVP.** Проект многосистемный, и расползание объёма — главный риск для соло-разработки. Задача выходит за MVP (`GDD.md` §11) — сказать об этом до начала.
7. **Чистая консоль не означает «работает».** Приёмка блока — замер в play mode, а не отсутствие ошибок. Отчёт 010, Р1: забег не стартовал вообще при полностью чистой консоли.
8. **Замер механики не означает «работает».** Приёмочный замер обязан включать величину, которую **видит игрок**, в единицах игры — метры, секунды, число врагов вокруг, — а не только факт срабатывания механики. «Компонент создан», «система отработала», «ошибок нет» — это проверка того, что код исполнился, а не того, что в игре что-то произошло. Отчёт 027, блок 5: замер показал «`Pushed` создан у всех задетых, пропусков 0, выстрелов нет» и был признан успешным — при том, что средний враг отлетал на **13 см** при собственной ширине 80 см и возвращался за 0.05 с, то есть в игре толчка не было вовсе. Данные для диагноза лежали в том же отчёте и были прочитаны в пользу блока.

---

## 7. Красные линии

- **Никаких `GameObject.Find`, `FindObjectOfType`, `static`-синглтонов.** Всё через DI.
- **Никаких аллокаций в горячем цикле симуляции.** LINQ, `new List<>`, боксинг, замыкания — вне тика.
- **Физика Unity не используется для поиска врагов.** Только свой spatial hash.
- **Никаких подписок R3 и `async` внутри ECS-систем.**
- **Никаких прямых правок статов.** Всё через пайплайн модификаторов — см. `ARCHITECTURE.md` §5. Систем-источников модификаторов пять, и без единой точки сборки числа станут неотлаживаемыми.
- **В конструкторе ECS-системы нельзя читать инжектируемые поля.** `EcsCustomInject`, `EcsPoolInject`, `EcsFilterInject` заполняются в `systems.Inject()`, то есть после `new`. Читать начиная с `Init`. Отчёт 010, Р2.
- **Игрок не хардкодится.** Все параметры — из `CharacterConfig`, с первого дня.
- **У всего контента — стабильные строковые ID.** Персонажи, способности, перки, предметы, апгрейды. Не имя ассета, не индекс в списке, не прямая ссылка. Патч, меняющий порядок или имя, обязан не ломать сейвы.
- **Сейвы всегда версионированы**, с первого сейва.
- **Юридические ограничения по лору** — см. `LORE.md` §5. Проверять при работе с любым визуалом и неймингом, включая иконки валют и предметов.

---

## 8. Известные технические риски

Осознанный техдолг. Пополнять по мере появления: каждое решение «пока так, потом переделаем» попадает сюда, а не в память разработчика.

- ~~**Идентификаторы сцен — строковыми литералами в коде**~~ (`BootstrapEntryPoint`, 2026-08-20) — **закрыто 2026-08-27** блоком 4 шага 1. Меню берётся из `ScenesConfig`, арена — из `LevelConfig` по стабильному ID через `ContentRegistry`. Литералов имён сцен в коде не осталось.
- **Имя сцены — непроверяемая строка в конфигах** (`LevelConfig.SceneName`, `ScenesConfig`, 2026-08-27). Наследник пункта выше в суженном виде: строка уехала из кода в данные, но осталась строкой. Переименование сцены в проекте по-прежнему ломается молча, в рантайме. Цена ниже прежней — таких строк ровно две, обе в ассетах, и обе видны в инспекторе. **Условие возврата:** редакторный валидатор, сверяющий имя со списком Build Settings, либо типизированная ссылка на сцену (обёртка с полем под `#if UNITY_EDITOR`, пишущая имя в обычную строку).
- **NuGet-dll пробивают границы сборок** (обнаружено 2026-08-20, **для `Game.Simulation` закрыто 2026-08-21**). Dll вне asmdef (пакеты NuGet в `Assets/Packages`) автоматически видны всем сборкам проекта: `using R3;` компилировался и в `Game.Bootstrap`, и в `Game.Simulation` без единой ссылки на пакет R3. У `Game.Simulation` выставлены `overrideReferences: true` и пустой `precompiledReferences` — теперь тот же `using` даёт `CS0246`, см. `ARCHITECTURE.md` §15. **Остаётся открытым для остальных сборок** осознанно: R3 нужен `Game.UI` и `Game.Meta` легально. **Условие возврата:** появление сборки, которой precompiled-библиотеки противопоказаны так же, как симуляции.
- **Редакторные типы в runtime-сборках компилятор в редакторе не ловит** (напоролись 2026-08-20). `using Unity.ProjectAuditor.Editor;`, добавленный автоимпортом IDE в `Game.Services`, компилировался в редакторе и упал бы только на сборке билда: модули `UnityEditor.*` автоматически подключаются к editor-компиляции любой сборки и отсутствуют в player-компиляции. Постоянного решения нет, кроме проверки — **делать пробный билд раньше, чем на шаге 9**. К 2026-08-29 добавились новые кандидаты на сюрприз в player-компиляции: ссылка `Unity.InputSystem` в `Game.Services` и сгенерированный `GameControls.cs`.
- **Запуск забега висит на отладочном компоненте меню** (`DebugRunStarter`, 2026-08-28). Меню по `GDD.md` §12 делается на шаге 9 и будет переписано целиком; до тех пор кнопка «Играть» обслуживается временным компонентом со сериализованными `levelId` и `characterId`. Он же тянет в `Game.UI` ссылки на `UnityEngine.UI` и `Unity.TextMeshPro`. **Условие снятия:** появление настоящего UI меню на шаге 9 — компонент и его объект в сцене удаляются, ссылки asmdef остаются.
- ~~**Лаунчер забега не защищён от повторного вызова**~~ (`RunLauncher`, тянулось с отчёта 009) — **закрыто 2026-09-14** задачей 3 отчёта 022, досрочно: срок стоял «до шага 8». Лаунчер держит флаг на время старта и отклоняет параллельный вызов исключением, называющим, что утекло бы; флаг снимается в `finally`, поэтому неудачный старт не заклинивает лаунчер. Замерено: два `StartAsync` подряд без ожидания дают один скоуп, один мир, одну камеру.
- **`IView` отдаёт наружу `Transform`** (2026-09-08). Свойство нужно ровно двоим: `ViewFactory` — чтобы уничтожить объект, и дырке передачи цели — чтобы отдать камере follow target. Но оно же возвращает любой будущей системе прямой доступ к `Transform` в обход интерполяции, и запись мимо `SetPosition` снова даст ступени — молча, без ошибки. Сегодня дисциплина стоит дешевле непрозрачного хендла. **Условие возврата:** второй потребитель `IView.Transform`, которому нужен не «уничтожить» и не «следовать».
- **Интерполяция — `MonoBehaviour` с `Update` на каждой вьюхе** (2026-09-08, **порог уточнён замером 2026-09-24**). Цена измерена профайлером: `ViewInterpolator.Update` — 0.13 мс на 46 вьюх, то есть **2.8 мкс на вьюху за кадр**; при 500 вьюхах это ≈1.4 мс на кадр. Блок 4 (пул) это сознательно не трогал: пул и смена модели обновления — две разные правки, и при общей приёмке регрессию не разделить, а пул централизует владение вьюхами и делает менеджер дешевле, чем он был бы до него. **Условие возврата:** `LiveCap` выше 150 — там 1.4 мс становятся заметной долей кадра. Тогда покадровые `Update` заменяются одним менеджером, обходящим массив.
- ~~**Стартовые враги описаны тремя полями `LevelConfig`**~~ (`StartingEnemy`, `StartingEnemyCount`, `EnemySpawnRadius`, 2026-09-09) — **закрыто 2026-09-10** блоком 2 шага 3. Три поля удалены из `LevelConfig`; «кто, сколько, как часто и на каком радиусе» живёт в `WaveTimelineConfig` (стабильный ID `waves.gameplay`), `LevelConfig` вернулся к описанию арены и камеры и ссылается на таймлайн одним полем.
- ~~**Границы арены — одно число в `LevelConfig`**~~ (`ArenaRadius`, 2026-09-10) — **сужено 2026-09-14** задачей 4 отчёта 022. Конфликт «кольцо спавна вырождается в дугу» снят числом: `ArenaRadius` поднят с 30 до 45 при `SpawnRadius = 30`, камера не тронута. Замерено на 6000 спавнов с ходячим игроком: прижатых 57.3% → 2.0%, ближайший спавн 13.16 м → 28.12 м от игрока, а доля спавнов внутри видимой области (27.03 м до дальнего угла экрана) — **46.3% → 0.0%**. То есть раньше почти половина врагов появлялась на глазах у игрока, теперь ни одного. Цена измерена и принята: сетка выросла с 4096 до 8836 ячеек, перестроение 0.0599 → 0.1181 мс при 60 сущностях и 0.0955 → 0.1526 мс при 500 — до 0.3 мс на кадр в худшем случае пяти тиков, около 2% бюджета. **Что осталось открытым:** настоящих границ у арены по-прежнему нет — сущности свободно уезжают за край, ограничен только спавн, а `Ground` остаётся мешем 60×60, то есть визуально меньше новой арены. **Условие снятия:** первая механика, которой граница нужна как стена (отбрасывание, телепорт, зона), либо приведение меша к радиусу 45.
- ~~**`SpawnPlayerSystem.Destroy` владеет вьюхами всех сущностей, а не только игрока**~~ (замечено 2026-09-09) — **закрыто 2026-09-24** блоком 4 шага 4. Владелец один: `ViewPool`. Он помнит всё, что выдал, и уничтожает это в `Dispose`; `SpawnPlayerSystem` перестал быть `IEcsDestroySystem`, его `Destroy` удалён целиком. **Заодно исправлена цена, записанная здесь блоком 3 неверно:** «около 30 мкс на `Object.Destroy` одной вьюхи» измеряло только пометку — `Object.Destroy` отложен до конца кадра, и само уничтожение стоит **0.61 мс на вьюху в dev-билде** (замер профайлером: маркер `Destroy` 13.25 мс на 26 вьюх). Возврат в пул вместо уничтожения стоит **0.047 мс**, то есть в 13 раз дешевле.
- **Рут-моушен включён на дочернем `Animator` у героя и у врага** (2026-09-09). `applyRootMotion = True` на узле `View` внутри `Berserk.prefab` и `Enemy_Devil.prefab`, тогда как `ViewInterpolator` пишет позицию в **корень**. Сегодня не стреляет — замерено, `localPosition` дочернего узла равна нулю у всех девяти вьюх, потому что клипы текущих контроллеров рут-моушен не дают. Выстрелит на первом контроллере с ходячим клипом, и симптом будет «картинка отстаёт от логики», который пойдут искать в интерполяции, а не в аниматоре. **Условие снятия:** появление анимации передвижения — тогда либо `applyRootMotion = False` осознанно, либо рут-моушен становится источником движения и это отдельное архитектурное решение. **Перепроверено 2026-09-25** блоком 6: первый клип, который играется поверх бега, — `Death` дьявола (0.7 с), и после него 36 вьюх выданы из пула повторно со сдвигом дочернего узла ровно 0 (позиция и поворот). Двигает ли узел сам клип во время проигрывания — не замерялось: замер снимался только на выданных, а не на умирающих. Пункт остаётся открытым до ходячего клипа.
- **Радиус запроса к пространственному индексу ограничен размером ячейки** (`SpatialGrid.Query`, 2026-09-13). Запрос обходит одно кольцо ячеек вокруг центра — 3×3. Это корректно ровно при `radius <= cellSize`; при большем радиусе обход молча терял бы цели, то есть урон не проходил бы по врагу, до которого достали. Пока потребителей индекса нет, правильное поведение — падать громко, и `Query` бросает `InvalidOperationException` с текстом, называющим починку. Сегодня `cellSize = 1.0` (`Level_Gameplay`), то есть легальный радиус запроса — не больше метра. **Условие снятия:** первая аура или оружие с радиусом больше ячейки (шаг 5), а также контактный радиус шага 4, если он окажется больше метра. Тогда выбор из двух: поднять `SpatialCellSize` в конфиге (дёшево, но дороже по времени тика — таблица замеров в отчёте 020 §7.4) либо расширить обход до `ceil(radius / cellSize)` колец (правка `Query`, цена запроса растёт квадратично по числу колец).
- **Толпа плотная на неподвижном игроке, хотя точку игрока больше не занимает** (2026-09-13, **сужено 2026-09-24** блоком 1 шага 4). Числа перезамерены 2026-09-24 на живом забеге в play mode по коммиту `2eddcfb`; прежние были сняты на зонде, откаченном вместе с `d319969`, и описывали код, которого в репозитории не было. Контактный радиус сделал ровно то, что обещал, и не больше: минимальная дистанция «враг ↔ игрок» на сиде 987654321, тик 3601, выросла с 0.0164 до 0.7583 при сумме радиусов 0.8 (остаток 0.041667 — ровно один тик хода врага при `MoveSpeed` 2.5). Пятеро из шестидесяти формально стоят ближе `contact − MoveSpeed·dt`, но глубже порога на 0.0000001 — это последний бит float, а не нарушение. Но стоячий случай по-прежнему отличается от подвижного: 9 занятых ячеек, максимум 14 в ячейке, минимальная дистанция «враг ↔ враг» 0.0866 — против 30 / 4 / 0.4099 у подвижного (числа подвижного случая перезамером 2026-09-24 не проверялись, взяты из отчёта 021). Причина, найденная блоком 4 шага 3, никуда не делась: вокруг игрока на окружности радиусом 0.758 помещается около шести тел, а давят туда все шестьдесят, и правило «сила разделения строго меньше единицы» гарантирует положительную проекцию на направление к цели, поэтому развернуть их подмешиванием нельзя. **Условие снятия:** взаимодействие «враг ↔ враг» с непроходимым контактом, а не подмешивание к намерению.
- **Ветка разрыва ничьей дорога ровно там, где она нужна** (`SeparateNeighborsSystem`, 2026-09-13). Совпадение позиций разрывается хешем от пары id с `Cos` и `Sin` на каждой такой паре. На разделённой толпе ветка почти не выполняется и система стоит 0.084 мс при 60 живых; на схлопнувшейся выполняется почти для всех пар и та же система стоит 0.61 мс, а стенд из 500 слипшихся врагов даёт 32.56 мс на тик — больше кадра. Сегодня это не стреляет: боевой `LiveCap` равен 60, а 0.61 мс укладывается в подшаг. **Перезамерено 2026-09-24 после контактного радиуса, на живом забеге в play mode по коммиту `2eddcfb`; прежние 0.505 и 32.04 сняты на зонде, откаченном вместе с `d319969`, и описывали код, которого в репозитории не было.** При 60 живых на неподвижном игроке система стоит **0.3575 мс** (там же `ResolveBodyOverlapSystem` — 0.0189 мс). Стенд из 500: **25.09 мс** после 60 тиков расселения и **54.04 мс** в момент, когда все 500 стоят ровно в одной точке — то есть на стенде цена не упала, а выросла к худшему случаю: контактный радиус выгоняет врагов с точки игрока на окружность, где помещается около шести тел, а стоят там все пятьсот, и совпадений позиций не убавилось (максимум в ячейке 124 из 500). Сравнивать эти числа со старыми 0.505 / 32.04 напрямую нельзя: способ замера тех чисел нигде не записан, отчёта 023 на диске не было. Нынешний способ — `Stopwatch` вокруг повторных `Run` на замороженном состоянии play mode, 200 повторов при 60 живых и 20 на стенде, после прогрева. **Условие снятия:** подъём `LiveCap` заметно выше 60 либо появление механики, массово сводящей врагов в одну точку. Починка выбирается тогда же из двух: кешировать направление ничьей вместо тригонометрии на каждой паре, либо разрывать совпадение один раз при рождении, сдвигая позицию спавна.
- ~~**Удалённая сущность остаётся в индексе до перестроения, и запрос по ней падает**~~ (2026-09-24) — **закрыто 2026-09-24** блоком 3 шага 4, коммит `9ad02fb`. Перестроение индекса переехало из конца группы 5 в конец группы 13, то есть строится после всех, кто рождает и удаляет сущности; протухшая запись стала невозможна по построению и не зависит от того, какая система удалит сущность в будущем. Оба режима отказа старой схемы перед починкой воспроизведены: **громкий** — `Cant touch destroyed entity` из `SpatialGrid.Query` (и только в редакторе, проверка под `#if DEBUG`); **тихий** — id ушёл следующей созданной сущности, и новый враг в 0.50 м от игрока при радиусе запроса 0.9 не вернулся запросом вовсе, без исключения. Цена переезда записана отдельным пунктом ниже.
- **Сущность, рождённая в тике, невидима для запросов до конца этого тика** (2026-09-24, **сужено 2026-09-24** переездом перестроения в конец группы 13). Индекс — снимок конца прошлого тика, поэтому новорождённый соседей не видит и соседям не виден ровно первый тик жизни, в какой бы группе он ни родился. Раньше этот пункт был опаснее и зависел от группы: спавн после перестроения (группа 5) выпадал из индекса до следующего тика, то есть будущий спавн снаряда или дропа из групп 6–12 молча не попадал бы в индекс. Теперь порядок групп на это не влияет. Остаток, который никуда не делся: **пачка гарантированного спавна рождается внахлёст** и начинает расходиться со второго тика. **Условие снятия:** механика, которой первый тик жизни важен — например, снаряд, обязанный попасть в цель в тик рождения. Тогда либо точечный `grid.Add` в месте рождения, либо второе перестроение.
- **Потребитель индекса после `MoveSystem` читает состав кандидатов с отставанием на шаг движения** (2026-09-24, цена переезда перестроения в конец группы 13). Раньше группы 6–13 читали индекс, построенный мгновением раньше в группе 5; теперь — снимок конца прошлого тика. Координаты при этом актуальные: в ячейке лежит только `int`, позиции запрос читает из пула. Условие корректности сильнее обычного `radius <= cellSize` ровно на смещение **кандидата**: **`радиус запроса + максимальное смещение кандидата за тик <= размер ячейки`**. Смещение центра запроса не считается — центр передаётся живой позицией. Сегодня кандидаты это враги: `0.9 + 2.5/60 = 0.942 <= 1.0`. Нарушается молча — потерей кандидата, а не исключением. **Точки перепроверки:** блок 5 шага 4 (расталкивание рывком делает врага кандидатом с большим смещением за тик) и любой будущий запрос после группы 5, где кандидатом может оказаться игрок — на рывке он проходит 1.43 м за тик, и условие ломается сразу. Рывок игрока не ломает условие для `DetectContactDamageSystem`: там игрок центр, а не кандидат. Тогда выбор из двух: поднять `SpatialCellSize` либо вернуть второе перестроение в конец группы 5, его цена измерена — 0.1310 мс при 60 живых.
- **Врагов убивает отладочный источник урона, а не оружие** (2026-09-24, блок 3 шага 4). Клавиша `K` — 20 урона по всем врагам в радиусе 3 м от игрока, числа константами в `DebugDamageSystem` (группа 6, Weapons — там, где потом встанет настоящее оружие). Ввод читается вне ECS: `DebugDamageInput` строит `InputAction` кодом, без правки `GameControls`, и держит латч, который система потребляет, — поэтому нажатие не теряется в кадрах без тика. `Source` у события — **игрок**, а не пустая ссылка: отладочный источник подменяет собой оружие, и будущий потребитель `Source` (ярость берсерка, шаг 5) не должен отлаживаться на данных, которых в бою не бывает. Перебор врагов прямой, а не через индекс: радиус 3 м больше ячейки, и `Query` на нём бросает по своему же контракту. **Условие снятия: первое оружие шага 5** — удаляются `DebugDamageSystem`, `DebugDamageInput`, `IDebugDamageInput`, регистрация в `RunLifetimeScope` и параметр в конструкторе `RunEntryPoint`.
- ~~**Рывок проезжает сквозь врагов, и контактная коррекция этого не ловит**~~ (2026-09-24) — **закрыто 2026-09-24** блоком 5 шага 4, коммит `2b0e390`. `SweepDashPushSystem` (группа 8) считает расстояние не до точки, а до отрезка «позиция игрока до шага → после», поэтому непросмотренного коридора больше нет. Замерено в dev-билде на 45 живых: 93 выборки «враг под заметённым отрезком», **пропущенных ноль** (было 13 из 29), максимальное смещение врага за тик **0.0747 м** против 0.762 до блока. Индекс для этого не используется — прямой перебор врагов, см. пункт ниже.
- **Боевые статы пишутся из конфига напрямую, мимо пайплайна модификаторов** (2026-09-24, блок 2 шага 4). Осознанное отступление от красной линии §7 «Никаких прямых правок статов». Три поля: `CharacterConfig._maxHealth` (100) и `CharacterConfig._hitInvulnerabilitySeconds` (0.5) пишутся в компоненты `MaxHealth` и `HitInvulnerability` в `SpawnPlayerSystem`, `EnemyConfig._contactDamage` (дьявол 10, тролль 20) и `EnemyConfig._maxHealth` (дьявол 20, тролль 60, добавлено блоком 3) — в `ContactDamage`, `Health` и `MaxHealth` в `EnemyFactory`. Пайплайна `ARCHITECTURE.md` §5 ещё не существует, а без HP блок боёвки не собирается; писать заглушку пайплайна под один источник дороже, чем переписать три присваивания потом. Цена: пока эти три числа — единственный источник, любой будущий модификатор (перк на HP, аура на урон) обязан идти в пайплайн, а не добавляться четвёртым прямым писателем — иначе отступление станет привычкой. **Условие снятия: шаг 5**, где пайплайн статов появляется вместе с оружием: `MaxHealth` становится производным статом, `Health.Current` остаётся тем, что пишет урон, и эти два поля, вероятно, разъедутся по времени жизни.
- **Числа боёвки — временные, поставлены ради играбельности, а не баланса** (2026-09-24). `GDD.md` §4 держит длительность i-frames как **TBD**, и блок 2 этот TBD не закрывает: 0.5 с выбраны так, чтобы стояние в толпе стоило 100 HP за 5 секунд и блок можно было щупать руками. Настраивается на самом деле не урон и не HP по отдельности, а их отношение `урон / длительность i-frames` — замерено: 20 hp/с независимо от того, сколько врагов касается. **Условие снятия: шаг 7** (`GDD.md` §13, баланс). До тех пор `GDD.md` §4 обязан сохранять пометку TBD — иначе placeholder превратится в дизайн-решение задним числом.
- **Иерархия вьюхи врага — 350 узлов, и это главный множитель всех цен вокруг вьюх** (2026-09-24, замерено блоком 4). `Enemy_Devil.prefab` — 350 `GameObject` (рига под скиннинг), `Troll` — 61, `Berserk` — 83. Все замеренные цены линейны по узлам: уничтожение ≈1.7 мкс на узел, создание ≈2.5 мкс на узел, и даже выдача из пула (`SetActive(true)` + `Animator.Rebind`) стоит **0.858 мс** — столько же, сколько `Instantiate` нового экземпляра. То есть пул снял цену уничтожения, но цену активации 350 узлов снять не может: она в данных, а не в коде. **Условие снятия:** облегчение префабов врагов при импорте модели (`Rig → Optimize Game Objects` или эквивалент), начинать с врагов, берсерка не трогать до появления точек крепления оружия. Проверять отдельным замером, не вместе с кодовыми правками.
- **Скорость толчка ограничена размером ячейки индекса, а не вкусом** (2026-09-24, блок 5 шага 4). Индекс описывает мир на конец прошлого тика, поэтому кандидат, сместившийся больше чем на `ячейка − радиус запроса`, может уйти из ячейки, в которой записан, и **`DetectContactDamageSystem` перестанет его видеть сразу после толчка** — молча. Бюджет: `0.9 + PushSpeed/60 ≤ 1.0`, то есть не больше 6 м/с; стоит **5 м/с** (`0.9747`), а не впритык — в проекте уже был баг на равенстве во float (длительность рывка, отчёт 011). `SweepDashPushSystem.Init` считает это сам и отказывается стартовать за границей, называя три починки. **Условие снятия:** появление `QuerySegment` в индексе либо подъём `SpatialCellSize` — тогда потолок пересчитывается, а не отменяется.
- **Заметающая проверка рывка перебирает врагов напрямую, мимо индекса** (2026-09-24, блок 5 шага 4). Точечными запросами отрезок при ячейке 1.0 накрыть нельзя: нужен радиус строго больше контакта (0.9), а он же сверху ограничен ячейкой за вычетом смещения кандидата. Прямой перебор при `LiveCap` 60 — 60 проверок «точка ↔ отрезок» на тик рывка, то есть шум. **Условие возврата:** подъём `LiveCap` заметно выше 60 либо появление многих заметающих сущностей одновременно (снаряды шага 5) — тогда O(N) на каждую перестанет быть шумом, и понадобится `QuerySegment`.
- **Вспышка попадания и дизолв героя — `MaterialPropertyBlock` на рендерерах вьюхи** (2026-09-25, блок 6 шага 4, **из гипотезы в факт 2026-09-25**). Вспышка перекрывает `_BaseColor`, `_Color` и `_EmissionColor`, дизолв — `_DissolveAmount` на 0.8 с; у вьюхи один общий блок, потому что `SetPropertyBlock` заменяет блок рендерера целиком. Вспышка сегодня **выключена данными** (`_hitFlashSeconds = 0`, решение разработчика), код цел. **Несовместимость с SRP Batcher — факт документации**, а не гипотеза: «The GameObject mustn't use MaterialPropertyBlocks» (Manual, SRPBatcher-Materials) и «this is not compatible with SRP Batcher… will likely result in a drop in performance» (ScriptReference, MaterialPropertyBlock). Пока вспышки нет, блок висит только на 13 рендерерах героя в последние 0.8 с забега — цена нулевая по смыслу. **Условие возврата:** включение вспышки обратно при атаке по ~20+ целям за тик (шаг 5) — тогда замер кадра Frame Debugger'ом и, если всплеск подтвердится, вспышка через значение в материале или per-instance свойство.
- **Дизолв героя — упрощённое освещение и мировой шум** (2026-09-25, блок 6). Растворяется только герой, явно, через `IView.Dissolve()` из `PlayDeathFeedbackSystem` (фильтр `Player`); враги умирают клипом `Death` и отлётом. В момент смерти материалы героя подменяются копиями шаблона `M_Dissolve` на шейдере `Game/Dissolve`. Два осознанных упрощения: (1) освещение шейдера — Lambert главного света с его тенями плюс `SampleSH`, без PBR, без дополнительных источников и без SSAO на самой модели, поэтому в кадр смерти вид героя заметно меняется; (2) шум триплан в **мировом** пространстве — на анимированных частях узор слегка плывёт (пространство объекта отвергнуто замером: меш тролля порядка 0.025 единиц при шкале 100 против 1:1 у берсерка). **Условие снятия:** арт-проход по фидбэку — свой lit-шейдер для всех моделей либо Shader Graph, если его запрет будет снят.
- **Префаб HUD лежит в `LevelConfig`** (2026-09-25, блок 6). Рядом с префабом рига камеры — по тому же прецеденту: презентационный префаб забега, который нужно откуда-то взять в скоупе забега. HUD при этом не свойство уровня, и второй уровень обязан будет указать тот же префаб. **Условие возврата:** второй `LevelConfig` либо шаг 9 (меню), где появится собственный конфиг UI.
- **Одна длительность жизни трупа на всех врагов** (`FeedbackConfig._enemyDeathSeconds = 2.0`, 2026-09-25, блок 6). Число подобрано под самый длинный клип — `Troll_Death` 1.458 с плюс полсекунды лежания; дьявол с клипом 0.7 с лежит 1.3 с. Труп исчезает скачком, без растворения. **Условие возврата:** враг с клипом смерти длиннее ~1.8 с либо жалоба, что короткие смерти висят, — тогда время переезжает в `EnemyConfig` или выводится из длины клипа.
- **Отлёт трупа — косметика во вьюхе, симуляция о нём не знает** (2026-09-25, блок 6). Сущность врага удаляется в тике смерти; направление передаётся во вьюху один раз (`IViewFactory.Retire`), дальше вьюха летит сама: гравитация, пол y = 0, трение. Разброс — `System.Random` пула без сида, то есть отлёт не воспроизводится между прогонами. Пол — плоскость y = 0 константой, а не геометрия арены. **Условие возврата:** неровный пол, стены арены, либо механика, которой нужно положение трупа (дроп на месте падения, шаг 7) — тогда отлёт переезжает в симуляцию.
- **`Game.Core` содержит типы `UnityEngine`** (2026-08-29). `Vector2`, `Vector3`, `Transform`, `GameObject` — ради `IInputService` и `IViewFactory`. Альтернатива (свой векторный тип и непрозрачные хендлы вместо `Transform`) стоит дороже, чем даёт, пока вьюхи не пулятся. **Условие возврата:** необходимость использовать `Game.Core` вне Unity.

---

## 9. Отчёты и передача контекста между чатами

Работа ведётся в Claude Projects: **один чат = один шаг роадмапа**. Между чатами не сохраняется ничего, кроме файлов проекта — поэтому отчёт обязателен.

**В конце каждой сессии Claude выдаёт отчёт текстом**, разработчик кладёт его в `docs/recons/`.

Именование: `NNN_краткий-заголовок.txt` — номер с ведущими нулями, 2–5 слов о сути. Пример: `docs/recons/003_enemy-spawn-spatial-hash.txt`

Содержание:
- Что делали, какой шаг роадмапа
- Созданные/изменённые файлы
- **Карта блока:** путь данных через системы, с файлами (§2)
- **Развилки:** что выбрано, что отвергнуто, цена
- **Сценарий ручной проверки:** что сделать в play mode и что должно получиться
- Принятые решения и почему (дублировать в журнал `PROGRESS.md`)
- Что проверено запуском, с числами
- Что осталось незакрытым, вылезшие риски
- Что делать в следующей сессии

**В начале сессии** Claude называет шаг, сверяет доки с кодом через MCP и сообщает о расхождениях **до** начала работы.
